# Chapter NN — <concept in three words>

Operation(s) from `docs/harnesses/OPERATIONS.md`: <numbers and names>.
Git tag: `chNN`. Run: `just chNN`.

## The limitation

<Short terminal exchange showing what the chat cannot do before this chapter.>

## The concept

<Three to eight plain sentences. What the idea is and why it fixes the limitation.
One line each on how opencode, pi and hermes-agent do it.>

## The change

Files touched in `git diff chMM..chNN`:

- `src/harness/<layer>/<file>.py` — <one line>

<For each file: an excerpt copied from the real file, then a short paragraph.>

## Run it

```bash
git checkout chNN
just chNN
```

Expected output:

```
<what the reader sees>
```

## Tests

`just test` runs <what the new tests prove>. No network is needed.

## What is still missing

<The limitation the next chapter fixes.>
