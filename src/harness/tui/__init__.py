"""Layer 1: what the person sees and types.

Below this sit the conversation, the model and the settings layers.
The app holds the loop, commands handle slash lines and render
shows each reply as it arrives.
"""
